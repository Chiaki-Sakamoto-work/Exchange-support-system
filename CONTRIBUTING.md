# 開発ガイドライン (CONTRIBUTING)

このプロジェクトへの貢献（開発）に関するルールをまとめています。

## 開発フロー
本プロジェクトでは **GitHub Flow** を採用しています。

1.  **Issue の作成**
    *   開発を始める前に、必ず対応する Issue を作成してください。
2.  **ブランチの作成**
    *   作成した Issue に紐づく形でブランチを作成します。
3.  **プルリクエスト (PR) の作成**
    *   作業が完了、または途中で相談したい場合に PR を作成してください。
4.  **レビューとマージ**
    *   `main` ブランチへマージするには、**1名以上の承認 (Approve)** が必須です。

## ブランチ命名規則
以下の形式で命名してください。

形式: `<種類>/<Issue番号>-<説明>`

### 種類
| 種類 | 内容 |
| :--- | :--- |
| `feat/` | 新機能追加 |
| `fix/` または `bugfix/` | バグ修正 |
| `hotfix/` | 緊急の修正 |
| `refactor/` | 機能変更なしのコード改善 |
| `docs/` | ドキュメントのみの変更 |
| `chore/` | ビルドツール、パッケージ管理などの雑務 |
| `release/` | リリース準備 |
| `test/` | テスト追加 |

**例:** `feat/1-add-login-function`

## コマンドメッセージ規則
ブランチ名と同様の接頭辞を使用して、意図が明確になるようにしてください。

*   `feat:` 新機能追加
*   `fix:` または `bugfix:` バグ修正
*   `hotfix:` 緊急の修正
*   `refactor:` 機能変更なしのコード改善
*   `docs:` ドキュメントのみの変更
*   `chore:` ビルドツール、パッケージ管理などの雑務
*   `release:` リリース準備
*   `test:` テスト追加

## コーディング基準
### バックエンド (Ruby on Rails)
*   **Style**: RuboCop のルールに準拠します。
*   **Test**: RSpec を使用し、モデルやリクエストのテストを記述してください。

### フロントエンド (React / TypeScript)
*   **Style**: ESLint および Prettier のルールに準拠します。
*   **Component**: 関数コンポーネントと Hooks を推奨します。

### インフラ / 環境
*   `docker-compose` を使用したローカル環境との差異を最小限に抑えてください。
